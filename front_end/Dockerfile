# Используем официальный образ Nginx
FROM nginx:alpine

# Копируем статическое содержимое в директорию веб-сервера Nginx
COPY . /usr/share/nginx/html

# Открываем порт 80, на котором Nginx работает по умолчанию
EXPOSE 80